logging:
  level:
    com.rag: info
    com.rag.domain.mapper: warn
  file:
    path: /var/log/rag-server

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false


# QUE Engine — Query Understanding & Execution
grpc:
  que:
    host: ${QUE_HOST:localhost}
    port: ${QUE_PORT:50055}
